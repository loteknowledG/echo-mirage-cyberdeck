/** Property Management services — persistence and integrations land here later. */

export type PropertyManagementServicePlaceholder = {
  ready: false;
};

export const propertyManagementServices: PropertyManagementServicePlaceholder = {
  ready: false,
};

import { PiEyeClosed } from "react-icons/pi";

/** Survey pane — server rail tab glyph. */
export function SurveyRailIcon({ className }: { className?: string }) {
  return <PiEyeClosed aria-hidden className={className} />;
}

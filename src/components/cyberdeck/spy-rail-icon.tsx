import { PiEyeClosed } from "react-icons/pi";

/** Spy (Espionage) pane — server rail tab glyph. */
export function SpyRailIcon({ className }: { className?: string }) {
  return <PiEyeClosed aria-hidden className={className} />;
}

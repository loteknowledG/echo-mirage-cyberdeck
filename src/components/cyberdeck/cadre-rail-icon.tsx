import { GiDarkSquad } from "react-icons/gi";

/** Cadre pane — server rail tab glyph (react-icons/gi). */
export function CadreRailIcon({ className }: { className?: string }) {
  return <GiDarkSquad aria-hidden className={className} />;
}

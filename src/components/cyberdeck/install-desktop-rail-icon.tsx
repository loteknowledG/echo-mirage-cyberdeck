import { MdInstallDesktop } from "react-icons/md";

/** Install pane — server rail tab glyph (react-icons/md). */
export function InstallDesktopRailIcon({ className }: { className?: string }) {
  return <MdInstallDesktop aria-hidden className={className} />;
}

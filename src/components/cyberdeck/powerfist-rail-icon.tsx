import { PiHandFist } from "react-icons/pi";

/** PowerFist (rola-dex) pane — server rail tab glyph (react-icons/pi). */
export function PowerfistRailIcon({ className }: { className?: string }) {
  return <PiHandFist aria-hidden className={className} />;
}

import { ImDownload } from "react-icons/im";

/** MUTHUR-LOAD pane — server rail tab glyph (react-icons/im). */
export function MuthurLoadRailIcon({ className }: { className?: string }) {
  return <ImDownload aria-hidden className={className} />;
}

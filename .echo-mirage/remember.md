# Echo Mirage Working Memory

Loaded rules for the current discussion:

- Keep the tab label in the pane header, not in the body.
- If a right-side label exists, mirror the left-side header label in the header band.
- Keep square cards square.
- Prefer current-only recovery over history or background watchers.
- Avoid extra refactor work that changes behavior or forces a revert.
- Keep Mirage lightweight and manual.

Current task:

- Refine the header layout without moving labels into the page content.
- Refactor only when the structure is exact and behavior stays stable.

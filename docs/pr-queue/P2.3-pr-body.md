## L-CYBERDECK-001 — Extraction PR

| Field | Value |
|-------|-------|
| **Slice** | P2.3 |
| **Queue position** | HEAD |
| **Work order** | [L-CYBERDECK-001](docs/work-orders/L-CYBERDECK-001-cyberdeck-app-extraction.md) (D2.5, D2.6) |
| **Conductor** | [L-CYBERDECK-001-CONDUCTOR](docs/work-orders/L-CYBERDECK-001-CONDUCTOR.md) |
| **Verify brief** | [VERIFY-L-CYBERDECK-001-P2.3](docs/verifications/VERIFY-L-CYBERDECK-001-P2.3.md) |
| **Judicial receipt** | `docs/verifications/JP-L-CYBERDECK-001-P2.3.md` |

### Baseline (branch from `main` @ `e8e9185`)

| Metric | Before slice | After slice | Ceiling |
|--------|-------------:|------------:|--------:|
| `cyberdeck-app.tsx` lines | 8,253 | _fill_ | _lower if dropped_ |
| Import lines | 148 | _fill_ | 152 |

**P2.3 line target:** ≤ ~7,050 (approx −1,200+). `handleSend` / `handleStop` must be **gone** from app.

### Deliverables

- [ ] `src/lib/muthur-core/muthur-chat-client.ts` — fetch `/api/cyberdeck-chat`, SSE stream parse, abort/steer helpers
- [ ] `src/features/cyberdeck/muthur/use-muthur-chat-send.ts` — `handleSend` / `handleStop` hook (wires chat state, send intents, uplink)
- [ ] `cyberdeck-app.tsx` delegates via `useMuthurChatSend`; no `handleSend` / `handleStop` function bodies in app

### Out of scope (FAIL if touched)

- Chat column JSX → `muthur-chat-column.tsx` (P2.4)
- Commander posture/mission/delegation handlers (P2.5)
- Gateway / operator / survey host modules
- `/api/cyberdeck-chat/route.ts` server split (P7)

---

## Summary

Extract **`handleSend` / `handleStop`** and the **LLM uplink client** (`fetch` + SSE + abort/steer) out of `cyberdeck-app.tsx`. Highest compile-win slice in P2. Zero behavior change.

---

## Developer checklist

- [ ] Branched `cursor/extract-p2.3-muthur-chat-send` from fresh `main`
- [ ] `pnpm exec tsc --noEmit` — exit 0
- [ ] `pnpm probe:cyberdeck-compile-scope` — exit 0
- [ ] `pnpm probe:muthur-command-console` — exit 0
- [ ] `pnpm probe:muthur-response-visibility` — exit 0
- [ ] Probe ceiling lowered only if lines actually dropped
- [ ] `git diff main...HEAD --stat` — scope matches slice only
- [ ] **Open GitHub PR** (only in-flight extraction PR)

---

## Tester / judicial checklist

- [ ] Independent verify per [VERIFY-P2.3](docs/verifications/VERIFY-L-CYBERDECK-001-P2.3.md)
- [ ] `JP-L-CYBERDECK-001-P2.3.md` with PASS/FAIL + evidence
- [ ] Manual smoke: help, clear, reload persistence; **LLM uplink** if provider configured; **stop** during stream

---

## Merge gate (human / tech lead)

- [ ] Judicial **PASS**
- [ ] Only open extraction PR (queue invariant)
- [ ] Merge = pop queue → CONDUCTOR advances to P2.4

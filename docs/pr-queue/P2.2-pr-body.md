## L-CYBERDECK-001 — Extraction PR

| Field | Value |
|-------|-------|
| **Slice** | P2.2 |
| **Queue position** | HEAD |
| **Work order** | [L-CYBERDECK-001](docs/work-orders/L-CYBERDECK-001-cyberdeck-app-extraction.md) (D2.3, D2.4) |
| **Conductor** | [L-CYBERDECK-001-CONDUCTOR](docs/work-orders/L-CYBERDECK-001-CONDUCTOR.md) |
| **Verify brief** | `docs/verifications/VERIFY-L-CYBERDECK-001-P2.2.md` _(create with slice)_ |
| **Judicial receipt** | `docs/verifications/JP-L-CYBERDECK-001-P2.2.md` |

### Baseline (branch from `main` @ `02414ce`)

| Metric | Before slice | After slice | Ceiling |
|--------|-------------:|------------:|--------:|
| `cyberdeck-app.tsx` lines | 8,414 | _fill_ | _lower if dropped_ |
| Import lines | 151 | _fill_ | 152 |

### Deliverables

- [ ] `src/features/cyberdeck/muthur/muthur-send-intents.ts` — parse/route clear, help, atlas, glyph, survey-connect intents
- [ ] `src/features/cyberdeck/muthur/use-muthur-send-intents.ts` — hook wiring send-intent dispatch
- [ ] `cyberdeck-app.tsx` delegates; intent routing logic removed from inline `handleSend` preamble where extracted

### Out of scope (FAIL if touched)

- `handleSend` / `handleStop` body beyond intent preamble (P2.3)
- Chat column JSX (P2.4)
- `muthurPosture`, `muthurMission`, `muthurDelegations`, commander handlers (P2.5)
- Gateway / operator / survey modules

---

## Summary

Extract MUTHUR **send intent routing** (clear, help, atlas, glyph, survey connect) into pure modules + hook. Zero behavior change — `handleSend` calls hook or intent router instead of inline parsing.

---

## Developer checklist

- [ ] Branched `cursor/extract-p2.2-muthur-send-intents` from fresh `main`
- [ ] `pnpm exec tsc --noEmit` — exit 0
- [ ] `pnpm probe:cyberdeck-compile-scope` — exit 0
- [ ] `pnpm probe:muthur-command-console` — exit 0
- [ ] `pnpm probe:muthur-response-visibility` — exit 0
- [ ] Probe ceiling lowered only if lines actually dropped
- [ ] `git diff main...HEAD --stat` — scope matches slice only

---

## Tester / judicial checklist

- [ ] Independent verify per VERIFY-P2.2 (all check IDs)
- [ ] `JP-L-CYBERDECK-001-P2.2.md` with PASS/FAIL + evidence
- [ ] Manual smoke: `muthur help`, reload persistence, clear chat

---

## Merge gate (human / tech lead)

- [ ] Judicial **PASS**
- [ ] Only open extraction PR (queue invariant)
- [ ] Merge = pop queue → CONDUCTOR advances to P2.3
